---
const pageTitle = "About Me";
const subtitle = "THis is the about page of Kolade's blog"

const identity = {
  firstName: "Kolade",
  country: "Nigeria",
  wifeName: 'Adenike',
  occupation: "Developer and Technical Writer",
  hobbies: ["coding", "writing", "sporting"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];
const sports = ['Football', 'Athletics', 'Boxing', 'Combat sports']

const happy = true;
const finished = true;
const goal = 2;
const started = 1;

// CSS Variables
const skillColor = "navy";
const sportColor = "crimson";
const fontWeight = "bold";
const textCase = "uppercase";

import BaseLayout from '../layouts/BaseLayout.astro';
---


    <BaseLayout pageTitle={pageTitle} subtitle={subtitle}>
        <p>Here are a few facts about me:</p>
        <ul>
            <li>My name is {identity.firstName}.</li>
            <li>I live in {identity.country} and I work as a {identity.occupation}.</li>
            {identity.hobbies.length >= 2 &&
                <li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li>
            }
        </ul>

        <p>The name of my wife is {identity.wifeName}.</p>

        <p>My skills are:</p>
        <ul>
            {skills.map((skill) => <li class="skill">{skill}</li>)}
        </ul>

        <p>And the sports I like are:</p>
        <ul>
            {sports.map((sport) => <li class="sport">{sport}</li>)}
        </ul>

        {happy && <p>I am happy to be learning Astro!</p>}
        
        {started === 1 ? <p>Indeed, I'm happy to start!</p> : <p>I'm not happy to start.</p>}

        {finished && <p>I finished this tutorial!</p>}

        {goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not 3 days.</p>}
    </BaseLayout>